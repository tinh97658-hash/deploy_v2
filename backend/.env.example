# Server
PORT=8081
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
TRUST_PROXY=false

# JWT
JWT_SECRET=replace-me
JWT_REFRESH_SECRET=replace-me-refresh

# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=vmu_quiz
DB_PORT=3306

# Redis (for OTP and caching)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Password policy
PASSWORD_HASH_ROUNDS=10
RESET_OTP_TTL=600
RESET_OTP_MAX_ATTEMPTS=8
RESET_REQ_MAX=5
RESET_ALLOW_TEMP_PASSWORD=false

# Email: Choose ONE of the two following auth methods

# 1) Gmail OAuth2 (recommended; no App Password required)
GMAIL_OAUTH_USER=
GMAIL_OAUTH_CLIENT_ID=
GMAIL_OAUTH_CLIENT_SECRET=
GMAIL_OAUTH_REFRESH_TOKEN=

# 2) Basic SMTP (e.g., SendGrid, Mailgun, SES, custom SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Common email settings
EMAIL_FROM=
# When true and SMTP is not configured or fails, send via Ethereal test and return previewUrl
EMAIL_FALLBACK_TEST=true
PORT=8888
#app hostname
HOST_NAME=localhost

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=your_database_name

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here
JWT_REFRESH_SECRET=your_super_secret_refresh_key_here
JWT_EXPIRES_IN=24h

# Environment
NODE_ENV=development