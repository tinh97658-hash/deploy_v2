# Frontend API base URL (include /api prefix if proxied at root)
# For local dev when backend runs on 8081 without proxy:
# REACT_APP_API_URL=http://localhost:8081/api

# For production (if frontend served by backend under same origin):
# REACT_APP_API_URL=/api
